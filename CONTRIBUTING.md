# コントリビューション

クローゼットへの貢献ありがとうございます。

## 開発

```bash
npm install
cp .env.example .env
npm run dev
```

## 注意

`.env`、`data/`、個人写真、生成アセット、API キーは絶対にコミットしないでください。

## 元プロジェクト

このリポジトリは [tandpfun/wardrobe](https://github.com/tandpfun/wardrobe)（MIT）をベースにした日本版です。大きな機能追加をするときは、元リポジトリの設計意図も確認してください。
